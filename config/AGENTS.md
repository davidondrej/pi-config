# Configuration examples

- These are opt-in templates, not live settings. Merge by key or model ID; never overwrite a user's private configuration.
- Include only public OpenRouter models. Keep credentials and internal services out.
- Preserve approved model tuning and document its limits in `../docs/setup.md`.
